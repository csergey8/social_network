type InitialStateSideBar = typeof initialState

const initialState = {};

const sidebarReducer = (state: InitialStateSideBar = initialState, action :any): InitialStateSideBar => {
  return state
}

export default sidebarReducer;